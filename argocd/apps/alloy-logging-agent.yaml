apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: alloy-logging-agent
  namespace: argocd

spec:
  project: chatops

  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: alloy
      targetRevision: "*"
      helm:
        valueFiles:
          - $values/alloy.yaml

    - repoURL: https://github.com/denisvivdenko/chatops-infra-base.git
      targetRevision: main
      ref: values
      path: values

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
 